This is a Very Simple TO-Do App

This is a simple app that can be useful to explore the roles of states and props in react. 

To explore this project, Clone the repository and run npm install in the command line.